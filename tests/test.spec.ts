describe("write your test here...", () => {
    beforeAll(() => {
        //One-Time Setup
        // put code here that should run just before all the following tests.
    });
    afterAll(() => {
        //One-Time Teardown
        // put code here that should run just after all the following tests.
    });
    beforeEach(() => {
        // put code here that should run just before each of the following tests but just after beforeAll.
    });

    afterEach(() => {
        // put code here than should run just after each of the following tests.
    });

    test('this should pass', () => {
        expect(1).toBe(2);
    });
});